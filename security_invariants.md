# CCD/NEXUS — متطلبات السلامة الرسمية الأولية

## 1. النموذج

لدينا مجموعة validators في حقبة `e` بقوة تصويت كلية `W`. يفترض البروتوكول أن قوة الخصم البيزنطية أقل من `W/3`، وأن كل validator صادق لا يوقّع تصويتين متعارضين على نفس `(epoch, domain, object_id, version)`. الشبكة قد تكون غير متزامنة مؤقتًا؛ لذلك نثبت السلامة دون حد زمني، ونثبت الحيوية فقط بعد تحقق شرط الاستقرار الجزئي.

الشهادة الصحيحة تتطلب قوة تصويت أكبر من `2W/3`. وبما أن أي شهادتين من هذه العتبة تتقاطعان في قوة تصويت أكبر من `W/3`، فالتقاطع يحتوي على validator صادق واحد على الأقل.

## 2. حالات العملية

```text
SEEN       := العملية وصلت، ولم يثبت توفرها بعد
AVAILABLE  := DAC صحيح
PREPARED   := نصاب المجال صوّت على نفس العملية والإصدار
JOIN_READY := كل المجالات المطلوبة تملك PREPARED متوافقًا
COMMITTED  := شهادة نهائية محلية أو شهادة دمج صحيحة
ABORTED    := شهادة إلغاء صحيحة، ولا يمكن أن تصبح العملية COMMITTED
EXPIRED    := انتهت حقبة القفل دون شهادة دمج
```

الانتقالات المسموحة:

```text
SEEN -> AVAILABLE -> PREPARED -> COMMITTED
PREPARED -> JOIN_READY -> COMMITTED
PREPARED -> EXPIRED -> ABORTED
AVAILABLE -> ABORTED    (فقط إذا ثبت فساد العملية أو انقضاء صلاحيتها)
```

لا يوجد انتقال من `COMMITTED` إلى حالة أخرى داخل نفس الحقبة.

## 3. invariants أساسية

### I1 — عدم وجود شهادتي نهائية متعارضتين

لا توجد شهادتان صحيحتان `C1` و`C2` في نفس الحقبة وعلى نفس `(domain, object_id, version)` إذا كانتا تثبتان عمليتين مختلفتين.

**الحجة:** كل شهادة تتطلب أكثر من `2W/3`. تقاطع الشهادتين أكبر من `W/3`. وبما أن قوة الخصم أقل من `W/3`، يضم التقاطع validator صادقًا. هذا validator كان سيضطر إلى توقيع عمليتين متعارضتين، وهو ممنوع بروتوكوليًا. إذن الفرض يؤدي إلى تناقض.

### I2 — عدم وجود double-spend

لا يمكن لعمليتين نهائيتين استهلاك نفس الإصدار `(object_id, version)` مع مخرجات مختلفة.

**الحجة:** يختزل double-spend إلى I1 لأن الإصدار المتوقع جزء من مجال التصويت، وليس مجرد حقل داخل حمولة غير موثقة. كل عملية ناجحة تنتج إصدارًا جديدًا، وأي عملية ثانية على الإصدار القديم تصبح متعارضة.

### I3 — ذرية العملية متعددة المجالات

إذا كانت العملية `op` تملك `JoinCertificate` صحيحة على المجالات `D`، فكل انتقالاتها في `D` نهائية معًا. وإذا لم تملكها، فلا يجوز لأي validator تقديم أي جزء منها على أنه commit نهائي.

**شرط التنفيذ:** لا يكتب Domain Executor نتيجة `PREPARED` إلى الحالة النهائية؛ يحتفظ بها في سجل مؤقت حتى وصول Join أو Abort. وتتحقق عقدة القراءة من `JoinCertificate` قبل إظهار الأثر.

### I4 — عدم قبول شهادة ببيانات غير متاحة

لا توجد شهادة `PREPARED` صحيحة لعملية لا يستطيع النصاب استرداد بياناتها أو التحقق من `payload_commitment`.

**شرط التنفيذ:** توقيع `PREPARE` مشروط بوجود DAC. لا يجوز فصل التوقيع عن محليًا عن فحص الإتاحة.

### I5 — عدم إعادة الاستخدام عبر الحقب

لا يمكن استخدام شهادة صحيحة من epoch سابق لتثبيت عملية في epoch لاحق دون مسار انتقال معتمد.

**شرط التنفيذ:** كل digest موقع يتضمن `epoch` و`protocol_version` وdomain separator؛ إعادة التهيئة تصدر `EpochTransitionCertificate`، ولا تُقبل الشهادة القديمة إلا كدليل حالة مرجعية.

### I6 — سلامة snapshot/pruning

إذا قبلت عقدة خفيفة snapshot بجذر حالة `R_e`، فكل كائن ظاهر في الحالة يمكن ربطه بشهادة نهائية وسلسلة إثبات إلى `R_e`. حذف الأحداث القديمة لا يحذف الدليل القابل للتحقق.

**شرط التنفيذ:** كل snapshot يتضمن root hash، ومرجعًا إلى آخر شهادة لكل domain، وتوقيع نصاب على الجذر.

## 4. خصائص الحيوية

لا يمكن ضمان زمن نهائية ثابت تحت انقطاع أو تأخير غير محدود. شرط الحيوية الأدنى:

1. يوجد بعد وقت ما `GST` حد تأخير `Δ` غير معروف مسبقًا.
2. تبقى قوة validators الصادقين فوق عتبة النصاب.
3. يستطيع العميل أو proposer إعادة نشر العملية.
4. لا يبقى قفل متعدد المجالات عالقًا إلى الأبد؛ timeout ينتج Abort أو يمرر العملية إلى epoch recovery.

بعد تحقق هذه الشروط، يجب أن تصل العملية الصحيحة إلى `COMMITTED` أو `ABORTED`، ولا تبقى في `PREPARED` إلى ما لا نهاية.

## 5. مواضع الخطر التي تحتاج model checking

| الموضع | السؤال الحرج |
|---|---|
| Join/Abort | هل يمكن لرسالة Abort متأخرة أن تلغي عملية لديها Join صحيح؟ |
| reconfiguration | هل يمكن أن تصوت لجنة epoch الجديد على عملية تتعارض مع commit من epoch السابق؟ |
| domain discovery | هل يستطيع العميل إخفاء مجال قراءة/كتابة ثم تجاوز قفل المجال؟ |
| duplicate parents | هل تؤدي DAG forks إلى قبول شهادات تختلف في dependency digest؟ |
| partial delivery | هل يمكن إصدار DAC ناقص ثم استخدامه لفرض حالة مختلفة على عقدة؟ |
| retries | هل تعيد المحاولة استخدام nonce بطريقة تجعلها عملية جديدة أم إعادة إرسال آمنة؟ |
| cross-domain cycles | هل يسبب ترتيب القفل الحتمي deadlock أو يسمح بترتيب مختلف بين validators؟ |
| committee sampling | إذا أصبحت اللجان محلية مستقبلًا، ما احتمال لجنة فاسدة وكيف يُثبت الحد؟ |

## 6. قاعدة الأمان الأساسية

لا تعتمد السلامة على العقوبات الاقتصادية أو سمعة المدققين. slashing أو التعويضات مفيدة للحوافز، لكنها لا تصلح خطأً منطقيًا في الشهادة. يجب أولًا إثبات I1–I6 ضمن نموذج خصماء محدد، ثم إضافة الحوافز والعضوية المفتوحة كطبقة منفصلة.

## المراجع

- [Zhang et al., “Reaching Consensus in the Byzantine Empire: A Comprehensive Review of BFT Consensus Algorithms”](https://arxiv.org/html/2204.03181v3)
- [HotStuff: BFT Consensus in the Lens of Blockchain](https://arxiv.org/abs/1803.05069)
- [Danezis et al., “Narwhal and Tusk”](https://arxiv.org/abs/2105.11827)
- [Blackshear et al., “Sui Lutris”](https://arxiv.org/abs/2310.18042)

## نتائج الاختبار الاستقصائي الأولي

شُغّلت اختبارات حتمية على حالات `f=1,2,3` وبأحجام لجان `n=4,7,10`. نجحت اختبارات تقاطع النصاب، وعدم إمكانية تكوين شهادتين متعارضتين إذا امتنع المدقق الصادق عن التوقيع المزدوج، وذرية Join للعمليات متعددة المجالات، وفصل الحقبة/المجال في digest. هذه النتائج تتحقق من منطق النصاب في النموذج المبسط فقط؛ ولا تمثل برهانًا كاملاً للبروتوكول، ولا تغطي إعادة التهيئة، أو الإتاحة، أو تنفيذ البرامج، أو نموذجًا زمنيًا للشبكة.

ملف الاختبار القابل لإعادة التشغيل: `ccd_sanity.py`.

مخرجات التشغيل:

```text
quorum_intersection: [(1, 4, 3, 'ok'), (2, 7, 5, 'ok'), (3, 10, 7, 'ok')]
no_two_conflicting_certificates: [(1, 4, 3, 'ok'), (2, 7, 5, 'ok'), (3, 10, 7, 'ok')]
cross_domain_atomicity: ok
domain_separation: ok
ALL TESTS PASSED
```

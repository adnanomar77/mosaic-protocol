# CCD/NEXUS — مواصفة معمارية أولية

## 1. الفكرة في جملة واحدة

**CCD/NEXUS** هو سجل موزع سببي لا يفرض ترتيبًا عالميًا لكل العمليات؛ بل يثبت كل عملية داخل **مجال التعارض** الذي تمسّه، ويستخدم شهادة تركيبية فقط عندما تمتد العملية إلى أكثر من مجال.

هذا ليس ادعاءً بأن الإجماع أصبح غير ضروري. الفكرة هي نقل الإجماع من مستوى "كل المعاملات في العالم" إلى مستوى "الموارد التي يمكن أن تتعارض فعليًا"، مع بقاء مسار BFT حتمي للعمليات المتنازعة أو العابرة للمجالات.

## 2. المبادئ غير القابلة للتفاوض

| المبدأ | القرار |
|---|---|
| الحالة | كائنات/موارد ذات إصدارات، لا رصيد عالمي واحد غير قابل للتقسيم |
| السببية | كل عملية تحمل مراجع إلى الإصدارات والشهادات التي تعتمد عليها |
| الترتيب | ترتيب جزئي افتراضي؛ ترتيب كلي فقط داخل مجال تعارض أو معاملة عابرة للمجالات |
| النهائية | حتمية بشهادة نصاب، لا تأكيد احتمالي كضمان أساسي |
| الأمان | نموذج Byzantine مع حد أقصى f من أصل 3f+1 في النسخة الأولى |
| الشبكة | السلامة حتى أثناء عدم التزامن؛ الحيوية بعد استقرار جزئي للشبكة |
| المشاركة | نسخة أولى بلجنة validators معروفة لتدقيق البروتوكول، وواجهة لاحقة لاختيار لجنة بالـ stake/VRF |
| التخزين | سجل أحداث قابل للتقليم، مع snapshots وإثباتات عضوية وحالة |
| البرمجة | انتقالات حتمية ذات صلاحيات كائنات؛ العقود العامة متعددة المجالات تأتي بعد إثبات المسار الأساسي |

يستند حد 3f+1 وشهادة 2f+1 إلى نموذج BFT التقليدي، بينما تستفيد البنية من فصل النشر عن القرار ومن مزايا التوازي في DAG-BFT، مع الاعتراف بأن ذلك يضيف تعقيدًا يجب إثباته لا إخفاؤه. [1] [2]

## 3. نموذج الحالة والبيانات

### 3.1 الكائن

الكائن هو وحدة الحالة التي يمكن أن تتغير مستقلًا عن بقية النظام. يمثل الكائن بالسجل التالي:

```text
Object {
  object_id: 32-byte identifier,
  version: uint64,
  owner_or_policy: capability/policy,
  payload_commitment: hash,
  domain_id: hash(object_id || policy),
  last_certificate: certificate_id
}
```

لا يُسمح للعملية بتغيير كائن إلا إذا ذكرت `(object_id, version)` المتوقع. ينتج التغيير إصدارًا جديدًا، وبذلك يصبح الإنفاق المزدوج أو التحديث المتنافس نزاعًا واضحًا على زوج محدد من الكائن والإصدار.

### 3.2 العملية

```text
Operation {
  op_id: hash(canonical_encoding(operation_without_signature)),
  epoch: uint64,
  signer: public_key,
  inputs: [(object_id, expected_version)],
  outputs: [new_object_descriptors],
  read_commitment: hash(canonical_read_set),
  write_commitment: hash(canonical_write_set),
  parent_certificates: [certificate_id],
  program_hash: hash(deterministic_program),
  nonce: uint64,
  signature: signature
}
```

في النسخة الأولى، ينبغي أن يكون `inputs` صريحًا. لا نسمح لعقدة التنفيذ باكتشاف وصول سري إلى كائنات أخرى ثم الادعاء بأن العملية محلية؛ أي وصول غير معلن يؤدي إلى رفض التنفيذ. هذا يحول التعارض من مفهوم غامض إلى علاقة قابلة للتحقق.

### 3.3 الحدث السببي

الحدث هو وحدة النشر وليس وحدة الإجماع النهائية:

```text
Event {
  event_id: hash(epoch || validator || local_sequence || payload_hash),
  creator: validator_id,
  parents: [event_id...],
  operations: [operation_id...],
  availability_proof: DAC,
  creator_signature: signature
}
```

كل validator يستطيع إنشاء أحداث بالتوازي. المراجع تشكل DAG سببيًا، لكن وجود DAG لا يعني وحده أن العملية نهائية؛ النهائية تحتاج شهادة توافق صريحة.

## 4. أنواع الشهادات

### 4.1 شهادة إتاحة البيانات — DAC

قبل التصويت على حالة عملية، يجب أن يثبت proposer أن بيانات العملية أو القطعة المشفرة متاحة لنصاب كافٍ. يمكن تنفيذ ذلك بإقرارات موقعة أو ترميز محو/erasure coding، مع إبقاء نوع التنفيذ مفتوحًا في النموذج الأولي. الهدف هو منع شهادة تشير إلى hash لا يستطيع المدققون استرداد محتواه.

### 4.2 شهادة استعداد المجال — Domain Prepare Certificate

تُنشأ عندما يتحقق validators من صحة العملية، وصحة آبائها، وعدم تغيير الإصدار المتوقع، وعدم وجود قفل/شهادة متعارضة على نفس `(object_id, version)`:

```text
PrepareVote = Sign(
  "CCD/PREPARE/v1" || epoch || domain_id ||
  object_id || expected_version || op_id ||
  dependency_digest
)
```

تُقبل الشهادة عند جمع توقيعات نصاب موزون يعادل أكثر من ثلثي قوة التصويت في اللجنة. لا يوقع validator عمليتين متعارضتين على نفس `(epoch, object_id, version)`.

### 4.3 شهادة نهائية محلية — Local Commit Certificate

لعملية أحادية المجال، تتحول شهادة الاستعداد إلى نهائية محلية بعد مرحلة commit قصيرة أو توقيع threshold ثانٍ على نفس digest. يُحفظ سجل منع double-signing، ويكون الالتزام قابلًا للتحقق دون إعادة تشغيل كل الأحداث السابقة.

### 4.4 شهادة الدمج — Join Certificate

لعملية تمس مجموعة مجالات `D = {d1, ..., dk}`، لا تكفي شهادات الاستعداد المحلية منفردة. تنشئ العملية **Join Certificate** يتضمن:

1. قائمة مجالات وكائنات العملية وإصداراتها المتوقعة.
2. شهادة استعداد مستقلة لكل مجال.
3. بصمة موحدة لجميع الشهادات.
4. توقيعات لجنة الدمج على أن جميع الشهادات متوافقة وأنه لم تُعتمد عملية متعارضة على أي إصدار.

الدمج لا يرتب المعاملات غير المرتبطة؛ إنه يثبت ذرية هذه العملية فقط. وإذا تعذر جمع شهادة كل المجالات، تبقى العملية `PENDING` أو تنتهي بشهادة إلغاء، ولا تصبح بعض آثارها نهائية وبعضها الآخر غير نهائي.

## 5. سير المعاملة

### 5.1 معاملة أحادية المجال

1. يوقع العميل العملية مع مجموعة المدخلات والإصدارات المتوقعة.
2. ينشرها validator في حدث سببي، ويثبت إتاحة البيانات.
3. تتحقق عقد المجال من التوقيع والبرنامج والمدخلات والآباء.
4. يصوت كل validator مرة واحدة على الإصدار المتنافس.
5. عند بلوغ النصاب، تصدر `LocalCommitCertificate`.
6. يصبح الانتقال نهائيًا ويمكن تقليمه من سجل الأحداث بعد دخوله في snapshot لاحق.

### 5.2 معاملة متعددة المجالات

1. يعلن العميل جميع المجالات والكائنات قبل التنفيذ.
2. تتحقق كل المجالات من المدخلات وتصدر `PrepareCertificate`، مع ترتيب قفل حتمي حسب `domain_id` لمنع deadlock.
3. يرسل proposer مجموعة الشهادات إلى لجنة الدمج.
4. تتحقق اللجنة من الاتساق، ومن أن كل شهادة تخص نفس العملية ونفس epoch، ثم تصدر `JoinCertificate`.
5. تُثبت العملية ذريًا في جميع المجالات، أو تُلغى ذريًا عند انتهاء lease/epoch.

### 5.3 التعارض

إذا وصلت عمليتان لنفس `(object_id, version)`، فلا يمكن الحصول على شهادتي commit متعارضتين ما دام validator الصادق لا يوقع مرتين، لأن أي مجموعتين بحجم أكثر من ثلثي اللجنة تتقاطعان في عقدة صادقة. العملية التي لا تحصل على شهادة تبقى مرفوضة أو تعاد بمحاولة على الإصدار الجديد.

إذا توقفت عملية بعد الاستعداد وقبل الدمج، تنتهي صلاحية القفل في epoch محدد. لا يحق للـ timeout وحده تغيير الحالة؛ يلزم `AbortCertificate` أو إعادة التزامن وفق قاعدة الرسائل الموقعة، حتى لا يستغل مهاجم اختلاف الساعات.

## 6. طوابق النظام

| الطابق | الوظيفة | ما لا يقرره |
|---|---|---|
| Transport/Gossip | نشر الأحداث والقطع وإعادة الإرسال | لا يقرر النهائية |
| Availability | إثبات أن البيانات قابلة للاسترداد | لا يقرر ترتيب الحالة |
| Causal DAG | حفظ السببية ومراجع الاعتماد | لا يجعل كل عقدتين متنافستين متوافقتين تلقائيًا |
| Domain Executor | التحقق والتنفيذ داخل مجال الكائن | لا ينفذ وصولًا غير معلن |
| Conflict BFT | حسم المنافسة داخل المجال | لا يرتب المجالات غير المرتبطة |
| Joiner | تركيب شهادات العمليات العابرة للمجالات | لا يصنع ترتيبًا عالميًا دائمًا |
| State/Snapshot | حفظ جذور الحالة، pruning، وإثباتات العضوية | لا يلغي شهادات النهائية |
| Governance/Epoch | تغيير اللجنة والسياسات والإصدارات | لا يكسر شهادات epoch سابق |

## 7. نموذج التهديد والضمانات

| التهديد | الضمان المقترح | الحالة |
|---|---|---|
| Double-spend على كائن | إصدار متوقع + تصويت واحد لكل validator + quorum intersection | قابل للإثبات في BFT committee |
| قائد كاذب/اقتراحان | لا يوجد قائد دائم؛ proposer لا يملك سلطة منفردة | يحتاج اختبار view/timeout |
| Equivocation | أدلة توقيع مزدوج ورفض الشهادة المتعارضة، مع slashing لاحقًا | لا تعتمد السلامة على slashing |
| Sybil | لجنة معروفة في النسخة الأولى؛ stake/VRF لاحقًا | غير محلول بالكامل في permissionless v1 |
| إخفاء البيانات | DAC قبل التصويت، مع chunks أو erasure coding | يحتاج اختيار تنفيذ وتجارب |
| Replay | epoch + nonce + op_id + domain separation | قابل للتنفيذ |
| عقدة خفيفة خادعة | شهادات threshold + Merkle/state proofs | يحتاج تعريف snapshot الرسمي |
| شبكة مقسمة | السلامة تستمر؛ الحيوية قد تتوقف حتى استقرار الشبكة | حد نظري مقصود |
| عقد خبيث يعلن read/write ناقصًا | access control حتمي ورفض الوصول غير المعلن | يتطلب VM مقيدة |
| تعارض عبر مجالات متعددة | Join Certificate وقفل حتمي وAbort Certificate | أصعب جزء ويحتاج model checking |
| إعادة تهيئة اللجنة | epoch certificates، تأخير التطبيق، وحماية شهادات الحقبة السابقة | يحتاج بروتوكول مستقل |

## 8. ما الذي يجعله أخف وأسرع؟

التحسين ليس في حذف التحقق، بل في حذف العمل غير الضروري. العمليات المستقلة لا تنتظر ترتيبًا عالميًا، ويمكن تنفيذها على مجالات مختلفة بالتوازي. البيانات تُنشر مرة ضمن أحداث سببية، بينما تحمل الشهادات hashes أو توقيعات مجمعة بدل نسخ المعاملة في كل مرحلة. التخزين يستطيع الاحتفاظ برأس كل مجال وإثباتات الحالة بدل سلسلة تاريخية واحدة ضخمة.

لكن الأداء لا يُفترض مسبقًا. إذا كانت 90% من العمليات تمس الحالة العالمية نفسها، فسوف يتصرف النظام قريبًا من BFT عالمي. أما إذا كانت العمليات موزعة على مجالات مستقلة، فينبغي أن يرتفع throughput مع عدد المجالات والعمال، مع بقاء كلفة كل مجال محدودة.

## 9. حدود صريحة

هذا التصميم لا يحقق تلقائيًا عضوية مفتوحة آمنة على الإنترنت؛ ذلك يتطلب مقاومة Sybil واختيار لجان واقتصادًا حافزًا. كما أنه لا يجعل العقود العامة ذات الحالة العالمية رخيصة؛ تلك العقود قد تختزل كل المجالات إلى مجال واحد. ولا يوفر الخصوصية من تلقاء نفسه؛ الالتزامات الصفرية أو MPC/ZK مرحلة لاحقة. وأي نتيجة أداء يجب أن تكون مقارنة عادلة مع Sui Lutris وNarwhal/Tusk وHotStuff على نفس عدد العقد وحجم الرسائل ونموذج الشبكة، لا اعتمادًا على أرقام من أوراق مختلفة.

## المراجع

1. [Zhang et al., “Reaching Consensus in the Byzantine Empire: A Comprehensive Review of BFT Consensus Algorithms”](https://arxiv.org/html/2204.03181v3)
2. [Raikwar et al., “SoK: DAG-based Consensus Protocols”](https://arxiv.org/html/2411.10026v1)
3. [Baudet et al., “FastPay: High-Performance Byzantine Fault Tolerant Settlement”](https://arxiv.org/abs/2003.11506)
4. [Blackshear et al., “Sui Lutris: A Blockchain Combining Broadcast and Consensus”](https://arxiv.org/abs/2310.18042)
5. [Danezis et al., “Narwhal and Tusk: A DAG-based Mempool and Efficient BFT Consensus”](https://arxiv.org/abs/2105.11827)
6. [Yin et al., “HotStuff: BFT Consensus in the Lens of Blockchain”](https://arxiv.org/abs/1803.05069)

# تصميم شبكة الاختبار والعضوية — CCD/NEXUS

## الهدف

المرحلة الجديدة ستنقل النموذج من استدعاءات داخل عملية واحدة إلى عقد مستقلة تتبادل رسائل TCP محلية مؤطرة. كل عقدة ستملك مفتاح Ed25519، وتستقبل عمليات ورسائل تصويت من عقد أخرى، وتتحقق من الشهادات قبل تطبيق الحالة. لن نستخدم اتصالًا مشتركًا بالذاكرة كبديل عن الشبكة.

## نموذج الشبكة

| العنصر | قرار النسخة التجريبية |
|---|---|
| النقل | TCP loopback، رسالة JSON مؤطرة بطول 4 بايت |
| العقد | عملية مستقلة لكل validator، مع event loop واحد |
| الزمن | monotonic clock محلي؛ لا تعتمد السلامة على تزامن الساعات |
| الأعطال | تأخير، إسقاط، إعادة إرسال، توقف عملية، وequivocation اصطناعي |
| الحقبة | membership snapshot ثابت لكل epoch |
| التجربة | localhost أولًا، ثم عناوين/منافذ مستقلة داخل sandbox |
| القياس | p50/p95/p99، bytes، الرسائل، retries، commits، aborts |

## العضوية ومقاومة Sybil

لن ندعي أن مجرد مفتاح عام يمنع Sybil؛ يستطيع المهاجم إنشاء مفاتيح غير محدودة. لذلك تستخدم النسخة التجريبية **سجل قبول موزون بالحصص/الضمان**. كل validator يحتاج إلى `deposit_id` فريد وقيمة stake لا تقل عن `min_stake`. الوزن التصويتي يساوي stake، ولا تُقبل شهادة عضوية بلا توقيع سلطة القبول في النسخة الأولى.

هذا ليس نظامًا permissionless مكتملًا. إنه نموذج اختبار قابل للتدقيق يثبت مبدأ مهمًا: تقسيم ميزانية مهاجم ثابتة إلى هويات أكثر لا يخلق وزنًا تصويتيًا جديدًا، وكل هوية إضافية تستهلك وديعة مستقلة. الانتقال لاحقًا إلى عضوية مفتوحة يتطلب عقدًا ذكيًا أو سجلًا اقتصاديًا لا مركزيًا، وآلية اختيار لجان، وتحليلًا لاحتمال لجنة فاسدة.

### قواعد القبول

1. `validator_id` و`public_key` فريدان داخل الحقبة.
2. `deposit_id` لا يُستخدم مرتين.
3. `stake >= min_stake`.
4. سجل القبول يوقعه authority key في النسخة الأولى.
5. كل عقدة تتحقق من شهادة القبول قبل إدخال validator إلى اللجنة.
6. النصاب يحسب على مجموع stake، لا على عدد المفاتيح.
7. يمكن وضع حد أقصى لحصة validator واحد لتجنب سيطرة هوية واحدة؛ هذا حد حوكمة وليس حلًا تلقائيًا.

## اختبار Sybil المقترح

إذا كانت ميزانية المهاجم `B` والحد الأدنى `S_min`، فإن الحد الأقصى النظري لهوياته المقبولة هو `floor(B / S_min)`، لكن مجموع وزنها لا يتجاوز `B`. نختبر أن:

- هوية واحدة بوزن 100 لا تصبح أقوى عند تقسيمها إلى عشر هويات بوزن 10.
- لا يمكن إنشاء هوية ثانية باستخدام `deposit_id` نفسه.
- لا يمكن تمرير شهادة عضوية معدلة أو موقعة بمفتاح غير سلطة القبول.
- تغيير العضوية لا يصبح فعالًا إلا عبر Epoch Transition Certificate.

## تجربة الشبكة الموزعة

يبدأ العميل العملية ويرسلها إلى عقدة دخول. تنشر العقدة العملية إلى peers، وكل peer يتحقق من التوقيع والعضوية وإتاحة العملية، ثم يرسل vote إلى أعضاء اللجنة. عند اكتمال النصاب، تنتشر الشهادة، وتتحقق كل عقدة مستقلة منها قبل تطبيق الحالة. ستُسجل الرسائل الخام وعدد المحاولات وأزمنة الاستلام، حتى لا نخلط زمن المعالجة المحلي بزمن الانتقال.

## شروط نجاح المرحلة

نجاح المرحلة لا يعني تفوقًا على الأنظمة الخارجية. يعني أن لدينا شبكة محلية مستقلة يمكنها:

- قبول عضوية موزونة ورفض Sybil المكرر.
- تنفيذ عملية أحادية المجال عبر رسائل حقيقية بين عمليات مستقلة.
- رفض شهادة متعارضة أو معدلة عند عقدة لا تثق بالمرسل.
- إكمال Join ذري أو Abort بلا تطبيق جزئي.
- قياس p95 والبايتات والرسائل تحت delay/drop/retry.

## حدود صريحة

المحاكاة على localhost لا تمثل الإنترنت. كما أن authority-signed admission ليس permissionless بالكامل، ولا يثبت أن stake نفسه غير قابل للتلاعب. هذه المرحلة تقيس قابلية تركيب البروتوكول مع النقل والعضوية، وليست تدقيقًا اقتصاديًا أو أمنيًا نهائيًا.

## ملاحظة حول baselines القابلة للتشغيل

يوجد مستودع مرجعي لـ HotStuff بلغة Rust مع مجلد benchmark، كما يوجد مستودع Narwhal/Tusk بلغة Rust مع سكربتات Python/Fabric لتشغيل testbed محلي من 4 عقد. يذكر مستودع Narwhal/Tusk صراحةً أن benchmark المحلي يستخدم شبكات وتشفيرًا وتخزينًا فعليًا، ولذلك يمكن استخدامه لاحقًا كمرجع تشغيل حقيقي، مع ضرورة تثبيت commit وإعدادات workload ومقارنة النتائج بحذر. [HotStuff repository](https://github.com/asonnino/hotstuff) [Narwhal/Tusk repository](https://github.com/asonnino/narwhal)

هذه المستودعات ليست جزءًا من النموذج الحالي بعد؛ المقارنة الموحدة الحالية تستخدم abstractions محلية، بينما الخطوة الأقوى التالية هي استنساخ commit ثابت وتشغيل benchmarks الرسمية إن سمحت البيئة والاعتماديات.

## تفاصيل تنفيذات baseline الرسمية

مستودع HotStuff المرجعي هو Rust ويستخدم تشفيرًا وشبكات وتخزينًا فعليًا، وREADME يذكر benchmark محليًا عبر Fabric لأربع عقد، مع إعدادات افتراضية تتضمن مدة 20 ثانية وحجم معاملة 512 بايت. المصدر: https://github.com/asonnino/hotstuff وملفه `README.md`.

مستودع Narwhal/Tusk المرجعي Rust مع benchmark Python/Fabric، ويقبل إعدادات `nodes`, `workers`, `rate`, `tx_size`, `faults`, و`duration`. مصدره: https://github.com/asonnino/narwhal وملفه `benchmark/README.md`. الهدف المقترح للمقارنة هو تثبيت 4 عقد، عامل واحد، حجم 512 بايت، ومدة قصيرة موحدة، ثم تسجيل throughput وp95 end-to-end.

تم تثبيت commits المرجعية محليًا: HotStuff `dc01ac8626a64306f6a76ae6f8914535dd090bdd`، وNarwhal `f5145b7219f62b4607be65822d4e3c13147ce778`. أدوات Rust/Clang/tmux/Fabric أصبحت متوفرة، لكن جلب crates من crates.io كان بطيئًا جدًا وتجاوز عدة مهلات؛ أُوقفت المحاولة مؤقتًا لإعادة تشغيلها بإعدادات Cargo أكثر تسامحًا.
